# v158 — Final production recovery UI

The normal button order is:

1. Verify known URLs / saved candidates
2. Run the missing query only
3. Enhanced historical recovery
4. New / unlinked Darpan records
5. Same-name identity collisions
6. Optional Firecrawl retry

The historical technical-audit lane remains backend-compatible for old runs but is deliberately not shown as a production step.

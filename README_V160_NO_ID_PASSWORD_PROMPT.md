# Frontend v160 — NGO ID registry without password prompt

- Removes the visible admin-password field from Karnataka Recovery.
- Historical NGO-ID backfill is one click.
- NGO-ID registry export is one click through the same-origin proxy.
- All other protected admin operations retain their existing controls.

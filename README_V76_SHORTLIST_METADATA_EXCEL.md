# Frontend v76 — Shortlist Metadata + Excel Decisions

Adds a safer Lead Pool shortlisting flow:
- Lead rows now show `source_tag` and `shortlisting_comment`.
- `Send for shortlisting` requires a source tag and comment.
- Added `Download Shortlisting CSV` and `Import Shortlisting CSV` for Excel-driven approval decisions.
- `Send for PM Shortlisting` still requires admin password and will not overwrite existing PM work.

# V105 — Cinematic Film Integration

Integrated the supplied v87 How It Works implementation into the latest V104 frontend base.

Changes:
- Replaced `app/how-it-works/page.tsx` with `how-it-works.page.v87.tsx`.
- Removed the prior v86 `.svf-` stylesheet block from the end of `app/globals.css`.
- Appended the supplied v87 `.svfa-` stylesheet block.
- Preserved all other frontend routes, functionality, landing-page refinements, and visual repairs.

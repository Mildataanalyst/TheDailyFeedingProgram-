{
  "service": "DFP 2.0 frontend",
  "version": "v159",
  "release_date": "2026-08-01",
  "railway_ready": true,
  "features": [
    "production-first Karnataka Recovery UI beginning at RUN_01",
    "worker built-in ownership guard and capacity display",
    "no external technical-test gate",
    "one-account Serper controls",
    "visible permanent NGO IDs",
    "ADMIN_PASSWORD server-only same-origin proxy",
    "Node 22 Railway build fix",
    "Karnataka Recovery polling and downloads forced through same-origin search proxy",
    "visible UI v159 marker for deployment verification"
  ],
  "tests": {
    "typescript": "22 TypeScript/TSX files parsed successfully",
    "next_production_build": "Not run locally because the sandbox npm mirror returned 404 for yocto-queue; package-lock and .npmrc point to public npm for Railway.",
    "railway_build_configuration": "npm ci once; build command npm run build"
  },
  "notes": [
    "Use Core Backend v88 and Search Worker v77.",
    "ADMIN_PASSWORD is server-only and must match both Python services."
  ]
}

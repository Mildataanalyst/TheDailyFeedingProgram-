# DFP 2.0 Frontend v75 — Preview Copy Refinement

Changes:
- Landing CTA says `Preview` instead of `Video Preview`.
- How-it-works slide 2 now says: `There are two ways to do this. This website can do both.`
- Part 1 is clearly `Searching the internet`.
- Part 1 explains both general search and bulk search under the same internet-discovery route.
- Part 2 is clearly `Add human leads`.
- Replaced remaining `platform` language in the preview story with `website` where relevant.
- Human leads copy is simpler: NGO name, contact number, referrer, then same collective pool.

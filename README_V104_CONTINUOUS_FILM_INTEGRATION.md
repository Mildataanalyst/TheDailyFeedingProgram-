# V104 — Continuous Film Integration

Integrated the supplied Fable-style continuous film preview into `/how-it-works`.

Changes:
- Replaced `app/how-it-works/page.tsx` with `how-it-works.page.v86.tsx`.
- Appended the self-contained `.svf-` stylesheet from `story-v86.css` to `app/globals.css`.
- Preserved the rest of the frontend and all existing routes/functionality.
